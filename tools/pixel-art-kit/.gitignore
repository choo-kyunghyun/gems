# all generated artifacts (regenerable); committed content is scripts + docs only
out/
__pycache__/
*.pyc

# local-only set (the ComfyUI diffusion drivers): a running ComfyUI server + GPU/models,
# kept out of the repo so extreme/experimental tests stay machine-local. See README.
local/

/**
 * Marker component: the entity is a raider, a human bandit enemy species. Presence is the
 * identity, with an empty payload; allegiance is separate.
 * @typedef {Object} Raider
 */
globalThis.Raider = "Raider";

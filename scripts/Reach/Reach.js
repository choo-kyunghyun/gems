/**
 * A region the player reaches once: its BBox is the region, and entering it reports `target` as
 * a "reach" objective and spends the entity.
 * @typedef {Object} Reach
 * @property {string} target  the objective target it reports
 */
globalThis.Reach = "Reach";

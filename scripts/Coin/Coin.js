/**
 * Marks a collectible coin — a non-solid sensor the player picks up on overlap.
 * Bare marker (no data); CollectibleSystem reads TriggerSystem's hits to collect
 * it and removes the entity. Each coin is worth 1.
 * @typedef {Object} Coin
 */
globalThis.Coin = "Coin";

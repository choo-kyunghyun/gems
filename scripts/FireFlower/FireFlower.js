/**
 * Marks a fire flower power-up sensor. Collecting it grants Fire state (shoot fireballs).
 * CollectibleSystem.collectPowerup reads this via TriggerSystem hits.
 * @typedef {Object} FireFlower
 */
globalThis.FireFlower = "FireFlower";

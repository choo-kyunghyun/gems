/**
 * Marks the level-end goal post — a non-solid sensor that wins the level when
 * the player overlaps it. Bare marker (no data); CollectibleSystem detects the
 * overlap via TriggerSystem's hits.
 * @typedef {Object} Goal
 */
globalThis.Goal = "Goal";
